#include <iostream>
using namespace std;

int main()
{
    int arr[12] = {2, 4, 7, 89, 12, 15, 16, 17, 18, 24, 26, 28};
    int first = 0, last = 11;
    for (int i = 0; i < 12; i++)
    {
        int mid = (last + first) / 2;
        if (arr[key] > arr[mid]{

                       })
    }
    return 0;
}